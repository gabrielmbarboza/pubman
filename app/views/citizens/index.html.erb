
<header>
  <span>
    <h1>Munic&iacute;pes</h1>
  </span>
  <span>
    <%= form_with url: search_path, method: :get do |form| %>
      <%= form.label('Pesquisar') %>
      <%= form.text_field(:search) %>
      <%= form.submit "Buscar" %>
    <% end %>
  </span>
</header>
<main>
  <div class="citizen-info">
    <span>
      <%= button_to '+ Novo municípe', new_citizen_path, method: :get, data: { turbo_frame: 'modal' } %>
    </span>
  </div>
  <div id="citizens">
  <%= render "citizens", citizens: @citizens, pagy: @pagy %>
  </div>
</main>
